[app]
&{app_hosts}

[migration]
&{migration_hosts}

[app:vars]
ansible_ssh_private_key_file=${ssh_private_key_file}
ansible_ssh_user=${ssh_user}
db_host=${db_host}
db_port=${db_port}
db_user=${db_user}
db_password=${db_password}

[migration:vars]
ansible_ssh_private_key_file=${ssh_private_key_file}
ansible_ssh_user=${ssh_user}
db_host=${db_host}
db_port=${db_port}
db_name=${db_name}
db_user=${db_user}
db_password=${db_password}
migrations_folder=${flyway_folder}